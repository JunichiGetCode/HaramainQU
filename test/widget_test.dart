import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('HaramainQu app smoke test', (WidgetTester tester) async {
    // Placeholder test — will be expanded
    expect(1 + 1, equals(2));
  });
}
